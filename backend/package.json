{
  "name": "captureos-backend",
  "version": "1.0.0",
  "description": "CaptureOS Express API — photos, gallery, settings, camera orchestration",
  "type": "module",
  "private": true,
  "main": "src/server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
